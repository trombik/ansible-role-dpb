---
galaxy_info:
  author: Tomoyuki Sakurai
  description: Creates `chroot` for bulk package build and configures `dpb` build environment
  company: Reallyenglish
  license: BSD
  min_ansible_version: 2.0
  platforms:
  - name: OpenBSD
    versions:
    - 6.0

  galaxy_tags:
    - dpb
#dependencies:
#  - { role: username.common, some_parameter: 3 }
